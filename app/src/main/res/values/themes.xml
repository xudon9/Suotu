<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- DayNight so the window background follows the system setting; without this the
         activity flashes white before Compose draws its first frame.
         An AppCompat parent is required because the activities extend
         AppCompatActivity (needed for per-app locale support). -->
    <style name="Theme.Suotu" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
